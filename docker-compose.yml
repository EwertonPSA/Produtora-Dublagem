version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: anime_produtora
    volumes:
      - ./database_in_docker:/var/lib/postgresql/data